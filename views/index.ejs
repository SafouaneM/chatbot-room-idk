<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Chat Room</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
<div class="container mx-auto px-4 py-8">
    <div class="bg-white rounded shadow p-6">
        <h2 class="text-2xl font-bold mb-4">Simple Chat Room</h2>

        <div id="register-section">
            <form id="register-form" class="mb-4">
                <label for="username-input" class="font-bold">Username:</label>
                <input id="username-input" type="text" class="border border-gray-300 rounded p-2 ml-2" placeholder="Enter your username">
                <button type="submit" class="ml-2 bg-blue-500 text-white font-bold py-2 px-4 rounded">Register</button>
            </form>
        </div>

        <div id="chat-section" class="hidden">
            <div id="chat-messages" class="h-72 overflow-auto mb-4 border border-gray-300 rounded p-2"></div>
            <form id="chat-form" class="flex mb-4">
                <input id="chat-input" type="text" class="flex-grow border border-gray-300 rounded p-2" placeholder="Type your message">
                <button type="submit" class="ml-2 bg-blue-500 text-white font-bold py-2 px-4 rounded">Send</button>
            </form>

            <form id="private-message-form" class="flex mb-4">
                <select id="user-list" class="border border-gray-300 rounded p-2 mr-2">
                    <option value="">Select user</option>
                </select>
                <input id="private-message-input" type="text" class="flex-grow border border-gray-300 rounded p-2" placeholder="Type your private message">
                <button type="submit" class="ml-2 bg-green-500 text-white font-bold py-2 px-4 rounded">Send Private</button>
            </form>

            <div id="private-messages" class="h-72 overflow-auto mb-4 border border-gray-300 rounded p-2"></div>
        </div>
    </div>
</div>
<script src="/socket.io/socket.io.js"></script>
<script src="/main.js"></script>
</body>
</html>